#include "main.h"
#include <stdio.h>
/**
 * _strlen - Returns the lenght of a string.
 * @s: pointer char
 * Return: 0.
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
		return (c);

}
